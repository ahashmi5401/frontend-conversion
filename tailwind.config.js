/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // must be 'class' for JS toggle
  content: ["./**/*.html"], // your HTML files
  theme: {
    extend: {},
  },
  plugins: [],
};